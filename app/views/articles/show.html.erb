<div class="article-details">

  <%# Renders the specified article's title, content, author, and date published %>

  <h1><%= @article.title %></h1>

  <p><strong>Content:</strong> <%= @article.content %></p>

  <p><strong>Author:</strong> <%= @article.author %></p>

  <p><strong>Date Published:</strong> <%= @article.date %></p>

  <%# Buttons to edit and delete the article %>

  <%# Link to delete the article %>
  <%= link_to "Edit", edit_article_path(@article), class: "btn btn-primary" %>

  <%# Link to delete the article %>
  <%= link_to "Delete", article_path(@article), class: "btn btn-primary", data: { turbo_method: :delete, turbo_confirm: "Are you sure that you want to delete this article?"} %>

  <%# Link to navigate back to the list of articles %>
  <%= link_to "Back to Articles", articles_path, class: "btn-primary" %>

</div>