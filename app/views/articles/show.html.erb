<h1><%= @article.title %></h1>

<p><%= @article.content %></p>
<p><%= @article.date %></p>

<%# 
  Create links to edit current article or delete (destroy) current article. 
  Confirm method enabled for destroy in case of accidental user click.
%>
<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>
