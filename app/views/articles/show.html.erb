<h1><%= @article.title %></h1>
<p> <i> Published by <%= @article.author %> on <%= @article.date %> </i> <p>
<p><%= @article.content %></p>
<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
    <li><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<%= link_to "Back", root_path %>

