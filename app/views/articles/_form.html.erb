<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="articleFormItemDiv">
  <%= form.text_field :title, :placeholder => 'Title', :class => "formInput"%>
  <%= form.text_area :content, :placeholder => 'Content', :class => "formInput contentInput"%>
  <%= form.text_field :author, :placeholder => 'Author', :class => "formInput" %>
  <%= form.date_field :date, :class => "formInput"%>
  <%= form.button "SAVE", type: "submit", :class=> "btn btnPrimary" %>
  </div>
<% end %>
