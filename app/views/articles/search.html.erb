<div class="container">
  <h1>Search Results</h1>
  <% if @articles.any? %>
    <%= render 'article_table' %>
  <% else %>
    <p>No articles found.</p>
  <% end %>
  <%= link_to 'Back to Articles', articles_path, class: 'btn btn-primary mb-3' %>
</div>