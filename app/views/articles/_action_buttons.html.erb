<div class="container">
  <div class="row fixed-bottom mb-3">
    <div class="col d-flex justify-content-start">
      <%# Render 'Edit' button only if show_edit_button is true (default to true if not specified) %>
      <% if local_assigns.fetch(:show_edit_button, true) %>
        <%= link_to 'Edit', edit_article_path(@article), class: "btn btn-primary me-2" %>
      <% end %>
      <%= link_to 'Back', articles_path, class: "btn btn-secondary" %>
    </div>
    <div class="col d-flex justify-content-end">
      <%= button_to 'Delete', @article, method: :delete,
                    class: "btn btn-danger" %>
    </div>
  </div>
</div>