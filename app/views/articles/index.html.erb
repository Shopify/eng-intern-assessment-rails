<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Encyclopedia App</title>
    <%= stylesheet_link_tag 'index.html.css' %>
</head>
<body>
    <header>
        <h1>Encyclopedia</h1>
    </header>

    <div id="large-container">

        <%= form_tag("/articles/search/", method: :get) do %>
            <%= text_field_tag(:query, params[:query]) %>
            <%= submit_tag("Search") %>
        <% end %>

        <ul id="article-list">
            <% @articles.each do |article| %>
                <li>
                    <a href="/articles/<%= article.id %>">
                        <%= article.title %>
                    </a>
                </li>
            <% end %>
        </ul>

        <%= link_to "New Article", "/articles/new/" %>

    </div>
</body>
</html>
