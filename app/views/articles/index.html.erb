<h1> Articles </h1>
<% @articles.each do |article| %>
    <h3 > <%= article.title %></h3>
    <h4 > <%= article.author %></h4>
    
    <h4 > <%= article.date %></h4>
    <p > <%= article.content %> </p>   
   <%= link_to "Read more", article_path(article) %>
<% end %>