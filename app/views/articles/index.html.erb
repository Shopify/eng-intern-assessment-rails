
<h1>Articles</h1>

<ul>
    #this line below will iterates over the @articles collection. It loops through each article in the collection.
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>
#this line will have a hyperlink with the text "New Article" that points to the new_article_path
<%= link_to 'New Article', new_article_path %>