<!DOCTYPE html>
<html>
  <head>
    <title>Encyclopedia(EngInternAssessmentRails)</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"%>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
          <a class="navbar-brand" href="<%= home_path %>">Encyclopedia</a>
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="<%= home_path %>"> Home </a>
              </li>
              <li class="nav-item">
              <a class="nav-link" href="<%= new_article_path %>">Create article</a>
              </li>
            </ul>
      </div>
    </nav>
    <div class="container">
    <%= yield %>
    </div>
  </body>
</html>
